---
import { twMerge } from "tailwind-merge";
const props = Astro.props;
---

<blockquote
  class={twMerge(
    props.class,
    "border-l-4 my-4 pl-3 border-l-indigo-400 [&>p]:py-2 bg-[#1e1e2e] rounded-md",
  )}
  {...props}
>
  <slot />
</blockquote>
