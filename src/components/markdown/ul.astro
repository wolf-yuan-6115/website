---
import { twMerge } from "tailwind-merge";
const props = Astro.props;
---

<ul
  class={twMerge(
    props.class,
    "list-disc leading-loose list-inside my-2",
  )}
  {...props}
>
  <slot />
</ul>
