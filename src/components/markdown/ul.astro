---
import { twMerge } from "tailwind-merge";
const props = Astro.props;
---

<ul
  class={twMerge(
    props.class,
    "list-disc leading-loose text-sm sm:text-base list-inside my-2",
  )}
  {...props}
>
  <slot />
</ul>
