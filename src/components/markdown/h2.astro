---
import { twMerge } from "tailwind-merge";
const props = Astro.props;
---

<h2
  class={twMerge(
    props.class,
    "text-xl md:text-2xl py-3 font-bold scroll-mt-24",
  )}
  {...props}
>
  <slot />
</h2>
