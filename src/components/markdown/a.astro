---
import { twMerge } from "tailwind-merge";
const props = Astro.props;
---

<a
  class={twMerge(
    props.class,
    "underline underline-offset-4 decoration-dotted hover:decoration-solid decoration-gray-400 decoration-1",
  )}
  {...props}><slot /></a
>
