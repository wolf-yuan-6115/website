---
import { twMerge } from "tailwind-merge";
const props = Astro.props;
---

<a
  class={twMerge(
    props.class,
    "underline underline-offset-4 decoration-dotted decoration-gray-400 hover:decoration-white decoration-1 transition-colors duration-100",
  )}
  {...props}><slot /></a
>
