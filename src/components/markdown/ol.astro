---
import { twMerge } from "tailwind-merge";
const props = Astro.props;
---

<ol
  class={twMerge(
    props.class,
    "list-decimal leading-loose list-inside my-2",
  )}
  {...props}
>
  <slot />
</ol>
